import React from "react";

const ChannelDescription = () => (
  <div className="channel-description" style={{ height: 40 }}>
    <p className="channel name">#random</p>
  </div>
);

export default ChannelDescription;
